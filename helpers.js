// Este archivos es exclusivo para crear funciones para utilizar en distintas partes de la app

// Vardump es para pasar las url a los href de los a en pug
exports.vardump = (objeto) => JSON.stringify(objeto, null, 2);
